package Encapsulation;

public class gettermethod {

	/*
	 * private variables can only be accessed within the same class (an outside
	 * class has no access to it). However, it is possible to access for public get
	 * and set methods.
	 */
	
	/*The get method returns the variable value,
	 * set method update the value.
	 */

}
